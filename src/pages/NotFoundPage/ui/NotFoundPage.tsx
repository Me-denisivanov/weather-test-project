import { Typography } from '@mui/material';
import { memo } from 'react';

export const NotFoundPage = memo(() => (
    <div>
        <Typography variant="h3" sx={{ textAlign: 'center', mt: 10 }}>Page not found</Typography>
    </div>
));
